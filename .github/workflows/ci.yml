name: CI

on:
  pull_request:
  push:
    branches:
      - master
      - "releases/**"

jobs:
  lint-test-build:
    permissions:
      contents: read
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: org.knime.js.pagebuilder

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # full history for SonarCloud

      - uses: pnpm/action-setup@v4
        with:
          package_json_file: org.knime.js.pagebuilder/package.json

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          cache-dependency-path: org.knime.js.pagebuilder/pnpm-lock.yaml
          node-version-file: "org.knime.js.pagebuilder/package.json"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Lint and format
        run: pnpm run ci:lint-format

      - name: Run coverage
        run: pnpm run coverage

      - name: Sonar scan
        if: github.actor != 'dependabot[bot]'
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          projectBaseDir: org.knime.js.pagebuilder

      - name: Type check
        run: pnpm run type-check

      - name: Build app
        run: pnpm run build:app

      - name: Build lib
        run: pnpm run build:lib

      - name: Build shadow app lib
        run: pnpm run build:shadow-app-lib

      - name: SBom
        run: pnpm run move-sbom

      - name: Audit
        run: pnpm run audit

      - name: Upload dist artifact
        uses: actions/upload-artifact@v6
        with:
          name: pagebuilder-dist
          path: org.knime.js.pagebuilder/dist/**
